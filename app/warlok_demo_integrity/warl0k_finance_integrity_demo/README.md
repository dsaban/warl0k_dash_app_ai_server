# WARL0K Finance Integrity Demo

Streamlit demo of an **Integrity‑First** control plane for AP + close.

## Run
```bash
pip install -r requirements.txt
cd app
streamlit run app1.py
```

## Persistent CSV database
Edits in the UI are saved back to CSV files under `app/data/`.
